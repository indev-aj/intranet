import 'package:flutter/material.dart';

class XTVT extends StatelessWidget {
  const XTVT({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('XTVT@MJII'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}