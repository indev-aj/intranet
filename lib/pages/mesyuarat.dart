import 'package:flutter/material.dart';

class Mesyuarat extends StatelessWidget {
  const Mesyuarat({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tempahan Bilik Mesyuarat'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}