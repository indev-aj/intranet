import 'package:flutter/material.dart';

class PortalDalaman extends StatelessWidget {
  const PortalDalaman({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Portal Dalaman'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}