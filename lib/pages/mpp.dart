import 'package:flutter/material.dart';

class MPP extends StatelessWidget {
  const MPP({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('E-Undi MPP'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}