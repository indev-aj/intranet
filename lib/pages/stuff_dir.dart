import 'package:flutter/material.dart';

class StuffDir extends StatelessWidget {
  const StuffDir({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stuff Directory'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}