import 'package:flutter/material.dart';

class StudDB extends StatelessWidget {
  const StudDB({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Student Database'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}