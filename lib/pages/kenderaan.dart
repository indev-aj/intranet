import 'package:flutter/material.dart';

class Kenderaan extends StatelessWidget {
  const Kenderaan({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tempahan Kenderaan'),
        backgroundColor: Color(0xFFDD4814),
      ),
      body: Container(),
    );
  }
}